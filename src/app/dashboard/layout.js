export const metadata = {
  title: "Dashboard | Neila",
  description: "Contact dashboard — view messages from your portfolio contact form.",
};

export default function DashboardLayout({ children }) {
  return children;
}
